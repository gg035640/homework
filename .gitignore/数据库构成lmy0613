create schema "BLOG";

CREATE TABLE "BLOG".myuser(
ID integer primary key,
NAME text,
password text, 
);

CREATE TABLE "BLOG".BOLG_ALL(
AUTHOR_ID integer,
author_name text,
article_id integer primary key,
title text,
context text,
image text
)；

create table "BLOG".info(
id int primary key,
sex text,
hometown text,
email text,
birthday text,
postcode text,
tel text,
foreign key(id) references "BLOG".myuser(id))
